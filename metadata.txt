[general]
name=SEEA EA Toolkit
qgisMinimumVersion=3.16
description=Generates SEEA Ecosystem Accounting extent, physical/monetary flow, and asset accounts from two or more time-series ecosystem type rasters. No third-party Python package required.
version=0.5
author=Custom build
email=noreply@example.com

about=Two Processing algorithms from a shared raster time series (classified ecosystem-type rasters, one per year, read entirely with QGIS's own raster provider - no rasterio, no GDAL Python bindings, no third-party package needed beyond numpy which QGIS already ships). (1) Extent account + change matrix (SEEA EA Table 4.1 layout), derived directly from pixel-level comparisons between consecutive rasters. (2) Physical and monetary ecosystem service flow accounts (supply side, by ecosystem type) plus a monetary ecosystem asset account (SEEA EA Table 10.1 style: Opening value / Enhancement / Degradation / Conversions / Revaluations / Closing value, reconciling exactly), reusing the strategicc package's own established EcosystemServices.csv and AssetValuationParams.csv schemas and NPV methodology - if you already maintain a strategicc/ST-Sim project you can point this at the same files. See README.md for schemas and usage.

category=Analysis
tags=seea,ecosystem accounting,extent account,change matrix,ecosystem services,flow account,asset account,npv,land cover,time series
hasProcessingProvider=yes
experimental=True
deprecated=False
server=False
